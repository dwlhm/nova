# nova-renderer-java

Android primitive registry embedded into generated apps as `NovaRenderer.java`.

Mirrors `runtime/nova-renderer-js`: `definePrimitive`, `hasPrimitive`, `primitive`.

Extension packages register via generated `NovaRendererExtensions.register(...)`.
